name: AI Agent

on:
  # ручной запуск из Actions
  workflow_dispatch:
    inputs:
      prompt:
        description: "Инструкция для агента (если запускаешь вручную)"
        required: true
        default: "Почини баг(и) и улучши код по описанию"
      files:
        description: "Какие файлы редактировать (через пробел)"
        required: false
        default: "bot.py"
      model:
        description: "Модель Anthropic"
        required: false
        default: "claude-3-5-sonnet-20241022"
  # автозапуск по метке на issue
  issues:
    types: [labeled]

permissions:
  contents: write        # коммиты/пуш
  pull-requests: write   # создание PR
  issues: write          # при желании комментить в issue

jobs:
  run:
    # запуск: вручную ИЛИ при добавлении метки "AI Agent" к issue
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'issues' && github.event.label.name == 'AI Agent') }}
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Aider
        run: |
          python -m pip install --upgrade pip
          pip install "aider-chat>=0.60.0"

      - name: Git identity
        run: |
          git config user.name  "ai-agent[bot]"
          git config user.email "ai-agent-bot@users.noreply.github.com"

      - name: Prepare inputs (title/body/files/model + default branch)
        id: prep
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TITLE="Manual run"
            BODY="${{ inputs.prompt }}"
            FILES="${{ inputs.files }}"
            MODEL="${{ inputs.model }}"
          else
            TITLE="${{ github.event.issue.title }}"
            BODY="${{ github.event.issue.body }}"
            FILES="bot.py"
            MODEL="claude-3-5-sonnet-20241022"
          fi

          # TITLE может быть многострочным — пишем как многострочную env-переменную
          echo "TITLE<<EOF" >> "$GITHUB_ENV"
          echo "$TITLE"     >> "$GITHUB_ENV"
          echo "EOF"        >> "$GITHUB_ENV"

          # BODY (многострочный) кладём в файл, чтобы не ломать YAML
          printf '%s\n' "$BODY" > prompt_body.txt

          echo "FILES=$FILES" >> "$GITHUB_ENV"
          echo "MODEL=$MODEL" >> "$GITHUB_ENV"

          # Определяем дефолтную ветку репозитория
          BASE=$(gh repo view --json defaultBranchRef --jq .defaultBranchRef.name)
          echo "BASE_BRANCH=$BASE" >> "$GITHUB_ENV"

      - name: Create branch
        id: branch
        run: |
          BRANCH="ai-agent/${{ github.run_id }}-${{ github.run_attempt }}"
          git checkout -b "$BRANCH"
          echo "BRANCH=$BRANCH" >> "$GITHUB_ENV"

      - name: Run Aider (apply fixes)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "❌ Нет ANTHROPIC_API_KEY в Secrets. Останавливаюсь."
            exit 1
          fi

          echo "🎯 Модель: $MODEL"
          echo "📝 Файлы:  $FILES"
          echo "🗒️  Заголовок: $TITLE"

          # Собираем единый многострочный промпт
          PROMPT="$TITLE

$(cat prompt_body.txt)"

          echo "---- PROMPT ----"
          echo "$PROMPT"
          echo "----------------"

          # ВАЖНО: вся команда в одну строку, без разрывов YAML
          aider --yes --model "$MODEL" -m "$PROMPT" $FILES

      - name: Commit and Push
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "no_changes=true" >> $GITHUB_ENV
            echo "ℹ️  Изменений нет — возможно, промпт не требовал правок."
          else
            git commit -m "AI: $TITLE"
            git push -u origin "$BRANCH"
            echo "no_changes=false" >> $GITHUB_ENV
          fi

      - name: Create Draft PR
        if: env.no_changes == 'false'
        run: |
          gh pr create \
            --base "$BASE_BRANCH" \
            --head "$BRANCH" \
            --title "AI: ${TITLE}" \
            --body "Автогенерированный PR от AI Agent.

**Модель:** $MODEL
**Файлы:** $FILES

> Источник:
> - событие: ${{ github.event_name }}
> - инициатор: @${{ github.actor }}" \
            --draft

      - name: Comment issue with PR link (optional)
        if: ${{ github.event_name == 'issues' && env.no_changes == 'false' }}
        run: |
          URL=$(gh pr list --head "$BRANCH" --json url --jq '.[0].url')
          gh issue comment ${{ github.event.issue.number }} --body "Открыл PR: $URL"

      - name: No changes note
        if: env.no_changes == 'true'
        run: echo "ℹ️  Aider не внёс изменений. Проверь PROMPT/FILES или переформулируй задачу."
